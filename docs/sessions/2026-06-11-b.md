# Session log — 2026-06-11 (session 2)

Branch: `claude/new-session-33gbv0` · Session: live-API verification, Kalshi
client rewrite, fee verification, first data ingestion.

## What happened (chronological)

1. **Connectivity preflight passed** — Kalshi, Gamma, CLOB, Neon all reachable
   (the Full-network policy from last session took effect for this session).
2. **Secrets were missing** — the environment didn't carry the env vars from
   last session; user re-pasted DATABASE_URL and an ANTHROPIC_API_KEY in chat.
   Fresh CRON_SECRET/ADMIN_TOKEN generated into `.env.local` (gitignored).
3. **Kalshi API wire format had changed** (caught by a new live validation
   harness, `scripts/validate-live-apis.ts`, before any ingestion):
   prices now decimal strings in dollars (`yes_bid_dollars`), counts as
   `*_fp` strings, orderbooks under `orderbook_fp`, candlesticks under
   `close_dollars`. Old integer-cent fields are gone — the old client would
   have ingested all-null prices and crashed on orderbooks. Client rewritten
   to normalize at the edge; `centsToProb` removed; consumers updated.
4. **Fee schedules verified** (Kalshi series API + Polymarket help center):
   Kalshi quadratic × 1 uniform (crypto premium removed); Polymarket taker
   fees only on sports buys (0.03), sells exempt, geopolitics free.
   `fees.ts` + learn-page copy updated; sports category added.
5. **Matching heuristics dry-run** (`scripts/match-dry-run.ts`): real
   cross-platform pairs found at 0.70 (2028 nominees), near-misses below
   the 0.7 auto-confirm floor. 140→33 candidates after dedupe fix.
6. **Database was empty** — the pasted connection string reaches a fresh
   `neondb` (last session's migrations evidently went elsewhere, possibly a
   different Neon branch). Both migrations applied after explicit user
   sign-off. Gotcha: `sql.unsafe()` in @neondatabase/serverless v1 silently
   does nothing when awaited — use `sql.query()` (errors.md updated).
7. **First ingestion succeeded**: sync-catalog 2,000 events + 16,147 markets
   (Kalshi) and 2,000 events + 28,783 markets (Polymarket) in 30s; snapshots
   400 rows/run; several spaced snapshot runs for history.
8. **Anthropic key has no credits** — match job 400s. Added graceful
   degradation: on LLM failure the job queues heuristic candidates for
   human review with naively proposed market links (1×1 and unique
   label-equality only), nothing auto-confirmed. 14 pairs queued.
9. **All pages smoke-tested 200** on the dev server: home, /markets,
   /admin/health (job log shows the run history incl. the credit failure),
   /admin/matches (queue populated), /learn, /paper.

## State at session end

- ✅ Code adapted to the real 2026 Kalshi API and pushed
- ✅ Neon DB migrated, seeded, and ingesting (catalogs + snapshots)
- ✅ Review queue populated; admin pages working
- ❌ Anthropic credits exhausted — LLM auto-matching blocked until topped up
- ❌ No confirmed pairs yet → home/markets show empty states until either
  the user confirms pairs in /admin/matches or credits enable the LLM pass
- ❌ Not deployed to Vercel yet (user-owned step; cron config is in repo)

## Next session: do this

1. If credits added: run match job → auto-confirms high-confidence pairs.
2. After confirmations exist: snapshot picks up linked markets (incl. Kalshi
   orderbook depth), pair pages `/e/[slug]` come alive.
3. Deploy to Vercel with env vars (DATABASE_URL, CRON_SECRET, ADMIN_TOKEN,
   ANTHROPIC_API_KEY) so crons take over from manual job runs.
